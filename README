This is a basic template for running an SPA app w/Angularjs wiht Flask as the backend.

Dependant on Python 3.2. I left out the Angular library, so you'll have to add your own.

To start, run python app.py
